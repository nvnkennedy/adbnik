# Runtime dependencies (mirrors pyproject.toml — single source of truth is pyproject.toml).
# Install the package:
#   pip install .
# Editable dev install (includes pytest):
#   pip install -e ".[dev]"

PyQt5>=5.15.11,<6.0
pyserial>=3.5,<4.0
paramiko>=3.0,<5.0
