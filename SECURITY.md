# Security policy

**Primary delivery is PyPI** (`pip install adbnik`) — standard Python package signing chain.

Report security issues via GitHub **private** security advisories on the repository, or open a confidential issue if you cannot use advisories.
